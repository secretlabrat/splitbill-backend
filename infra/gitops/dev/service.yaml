apiVersion: v1
kind: Service
metadata:
  name: split-bill-dev-service
  namespace: split-bill-dev
spec:
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: api
  type: ClusterIP
